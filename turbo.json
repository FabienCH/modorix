{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dists/**"]
    },
    "build:dev": {
      "dependsOn": ["^build"],
      "outputs": ["dists/**"]
    },
    "lint": {},
    "test": {},
    "test:ci": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
